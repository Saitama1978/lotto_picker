name: Build Android APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.16.0'

      - name: Clean Flutter Project
        run: |
          cd lotto_picker
          flutter clean

      - name: Auto-Fix Pubspec Dependencies
        run: |
          cd lotto_picker
          # Papalitan natin ng "any" ang firebase_core at firebase_core_web para si Flutter na ang mamili ng compatible version
          sed -i 's/firebase_core:.*/firebase_core: any/g' pubspec.yaml
          sed -i 's/firebase_core_web:.*/firebase_core_web: any/g' pubspec.yaml

      - name: Install dependencies
        run: |
          cd lotto_picker
          flutter pub get

      - name: Build APK (Final Legacy Release)
        run: |
          cd lotto_picker
          flutter build apk --release --no-sound-null-safety

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: lotto_picker/build/app/outputs/flutter-apk/app-release.apk
