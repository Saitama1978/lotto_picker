name: Build Android APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'

      # Tamang daan: papasok sa iisang folder lang
      - name: Install dependencies
        run: |
          cd lotto_picker
          flutter pub get

      # Dito na iluluto ang APK
      - name: Build APK
        run: |
          cd lotto_picker
          flutter build apk --release

      # I-upload ang pinal na file para ma-download mo
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Lotto-Asintado-Release-APK
          path: lotto_picker/build/app/outputs/flutter-apk/app-release.apk
